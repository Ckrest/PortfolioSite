#!/usr/bin/python3
"""Installed command entry point; imports only its maintained Site package."""
from pathlib import Path
import sys
sys.path.insert(0, str(Path(__file__).resolve().parents[1] / 'lib'))
from portfolio_site_control import main
if __name__ == '__main__':
    raise SystemExit(main())
