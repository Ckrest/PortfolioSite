blocks:
- body: '## Use the report’s project as repository context


    Work Report now carries the project named on a report into every Git lookup. When
    that project maps to a VCSH repository, Git runs through `vcsh <project>`; conventional
    repositories continue through Git directly.


    This connects reporting to Constellation’s package ownership structure, so the
    report selects the repository responsible for the work instead of treating the
    shared filesystem as one Git boundary.'
  id: blk_editorial_work-report-vcsh-support
  type: text
- code: "flowchart LR\n  Report[Report project identity] --> Route{Repository adapter}\n\
    \  Route -->|Standard repository| Git[Direct Git]\n  Route -->|VCSH package| VCSH[Package\
    \ VCSH repository]\n  Git & VCSH --> Facts[Commit + diff facts]\n  Facts --> Evidence[Stored\
    \ report evidence]"
  description: The report’s project identity selects one repository route for every
    Git fact and stored piece of evidence.
  id: blk_vcsh_reporting
  presentation: content
  sourceMode: inline
  type: mermaid
- body: '## Keep every recorded Git fact on the same route


    The statistics collector and Git evidence handler now use the same repository
    context for commit lookup, file changes, diff text, and commit messages. The numerical
    summary and retained evidence therefore come from the same package history.'
  id: blk_vcsh_aligned_evidence
  type: text
date: '2026-01-30'
part_of: doc_a67b241ca49f5f12ba0dcb4badd5ceac
prominence: low
related_to:
- doc_ed9b9a567c7c4d589dbe92c7abbf3eb6
summary: Work Report now uses the report’s project identity as the repository context
  for commit metadata, diff statistics, and captured Git evidence, routing VCSH-backed
  packages through their independent histories.
tags:
- Python
- Git
- Infrastructure
title: Made Work Report Git tracking package-aware
