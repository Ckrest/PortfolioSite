{
  "schema": "portfolio-update@3",
  "update": {
    "kind": "update",
    "slug": "monitor-detect",
    "title": "Shared the active Wayland output with session services",
    "summary": "Built a Bash startup script that waits for Wayland, selects the first enabled output from wlr-randr, and publishes it through both systemd and a runtime file.",
    "date": "2026-01-15",
    "prominence": "low",
    "discovery": "listed",
    "related_to": [
      "wallpaper-service"
    ],
    "github": "https://github.com/Ckrest/monitor-detect",
    "icon": "icon.svg",
    "tags": [
      "Bash",
      "Wayland",
      "Systemd",
      "Desktop Environment"
    ],
    "content": {
      "blocks": [
        {
          "body": "## Publish one output name for the session\n\nUser services needed a Wayland connector name, but hardcoding one tied them to a particular display setup. I added a startup script that waits for the Wayland socket, asks `wlr-randr` for enabled outputs, and selects the first one marked `Enabled: yes`.",
          "id": "blk_editorial_monitor-detect",
          "type": "text"
        },
        {
          "caption": "Representative output showing the environment and runtime-file forms of the detected connector.",
          "commands": [
            {
              "command": "./monitor-detect.sh",
              "output": "Detected primary monitor: DP-3\nExported: PRIMARY_MONITOR=DP-3\nWrote: /run/user/1000/primary-monitor"
            },
            {
              "command": "systemctl --user show-environment",
              "output": "...\nPRIMARY_MONITOR=DP-3\n..."
            },
            {
              "command": "cat \"$XDG_RUNTIME_DIR/primary-monitor\"",
              "output": "DP-3"
            }
          ],
          "id": "blk_monitor_detect",
          "type": "terminal"
        },
        {
          "body": "## Serve both systemd and file-based consumers\n\nThe script publishes the connector as `PRIMARY_MONITOR` in the systemd user environment and writes the same value to `$XDG_RUNTIME_DIR/primary-monitor`. Wallpaper Service reads the runtime file during startup, while other user services can consume the environment value after detection completes.",
          "id": "blk_monitor_consumers",
          "type": "text"
        }
      ]
    },
    "folder": "monitor-detect",
    "phase": 1,
    "preview": null,
    "previewAlt": "Shared the active Wayland output with session services",
    "relationships": {
      "parts": [],
      "superseded_by": [],
      "related": [
        {
          "slug": "wallpaper-service",
          "folder": "wallpaper-service",
          "title": "Built a wallpaper service with seamless switching",
          "summary": "Built a Wayland wallpaper daemon that hot-swaps video, image, and solid-color backgrounds without exposing a blank frame between changes.",
          "date": "2025-12-22",
          "prominence": "medium"
        }
      ]
    }
  }
}
